extends layout

block content
  div.page-header
    h1 Tournament list

  if !tournaments
    p no tournaments yet!
  else
    ul.thumbnails
      for entry in tournaments
        li.span3
          div.thumbnail
            img(src='http://placehold.it/260x130', style='margin-right: 10px;')
            h4 #{entry.title}
              small  Open for enrollment
                | #{entry.enroll}
            a(href='/tournament/' + entry._id).btn Details
